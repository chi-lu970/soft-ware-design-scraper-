# Design Pattern 參考整理文件

> 本文件整理 GoF（Gang of Four）所定義的 23 種設計模式，
> 依「創建型」、「結構型」、「行為型」三大分類分組說明。
> 每個模式包含：一句話定義、使用時機、核心角色。

---

## 一、創建型模式（Creational Patterns）

> **共同目的**：抽象化物件的建立過程，讓系統不依賴具體的類別。

---

### 1. Simple Factory（簡單工廠模式）

**一句話定義**：用一個工廠方法根據參數決定要建立哪種物件。

**使用時機**：
- 物件建立邏輯複雜，不想讓客戶端直接 `new` 物件
- 物件種類有限且固定，未來變化不大
- 想把建立邏輯集中管理

**核心角色**：
- `Factory`：根據條件建立並回傳對應的 Product
- `Product`：被建立的物件

---

### 2. Factory Method（工廠方法模式）

**一句話定義**：定義建立物件的介面，讓子類別決定要實例化哪個類別。

**使用時機**：
- 父類別不知道要建立哪種子類別
- 想讓子類別自行決定如何建立物件
- 框架或庫需要讓使用者擴展建立邏輯

**核心角色**：
- `Creator`（抽象）：宣告 `factoryMethod()`
- `ConcreteCreator`：實作 `factoryMethod()` 回傳特定 `Product`
- `Product`（介面）：被建立物件的型別

---

### 3. Abstract Factory（抽象工廠模式）

**一句話定義**：提供一個介面，讓客戶端建立一系列相關聯的物件，而不需指定具體類別。

**使用時機**：
- 系統需要跨越多個產品族（如 Windows UI vs Mac UI）
- 需要確保同族產品一起使用
- 想隔離客戶端與具體產品的依賴

**核心角色**：
- `AbstractFactory`：宣告建立各種產品的方法
- `ConcreteFactory`：實作特定產品族的建立邏輯
- `AbstractProduct`：每種產品的抽象介面
- `Client`：只透過抽象介面使用產品

---

### 4. Builder（建造者模式）

**一句話定義**：將複雜物件的建構步驟分離，讓相同的建構流程可以建立不同的表現形式。

**使用時機**：
- 物件需要多個步驟才能建構完成
- 物件有多種不同的「配方」（如各種漢堡套餐）
- 想避免建構函式有過多參數（telescoping constructor）

**核心角色**：
- `Builder`（介面）：宣告各建構步驟
- `ConcreteBuilder`：實作各步驟，最後提供 `getResult()`
- `Director`：按特定順序呼叫 Builder 步驟
- `Product`：最終建立的複雜物件

---

### 5. Prototype（原型模式）

**一句話定義**：透過複製（Clone）現有物件來建立新物件，而不是從頭建立。

**使用時機**：
- 物件建立成本高（如需要大量 I/O 或運算）
- 想建立一批類似但不完全相同的物件
- 物件的類別在執行期才確定

**核心角色**：
- `Prototype`（介面）：宣告 `clone()` 方法
- `ConcretePrototype`：實作 `clone()` 複製自身
- `Client`：呼叫 `clone()` 取得新物件

---

### 6. Singleton（單例模式）

**一句話定義**：確保一個類別只有一個實例，並提供全域訪問點。

**使用時機**：
- 需要唯一的共享資源（如設定管理員、Logger、連線池）
- 需要全域狀態，且希望避免重複建立開銷
- 協調多個子系統的工具類別

**核心角色**：
- `Singleton`：私有建構子 + 靜態 `getInstance()` 方法 + 靜態唯一實例

---

## 二、結構型模式（Structural Patterns）

> **共同目的**：處理類別和物件的組合方式，形成更大的結構。

---

### 7. Adapter（轉接器模式）

**一句話定義**：將一個類別的介面轉換為客戶端期望的另一個介面，讓不相容的類別能夠合作。

**使用時機**：
- 想使用一個現有類別，但其介面不符合需求
- 整合第三方函式庫或遺留程式碼
- 想建立可重用的類別，使其與不相關的類別協作

**核心角色**：
- `Target`（介面）：客戶端期望的介面
- `Adaptee`：現有的（不相容的）類別
- `Adapter`：包裝 `Adaptee`，實作 `Target` 介面

---

### 8. Bridge（橋梁模式）

**一句話定義**：將抽象部分與實作部分分離，使兩者可以獨立地變化。

**使用時機**：
- 想避免「抽象」與「實作」之間形成永久綁定
- 兩個維度都需要獨立擴展（如形狀 × 顏色）
- 在多個物件間分享實作，且客戶端不感知

**核心角色**：
- `Abstraction`：持有 `Implementor` 參考，定義高層操作
- `RefinedAbstraction`：擴展 `Abstraction`
- `Implementor`（介面）：低層實作的介面
- `ConcreteImplementor`：實作 `Implementor`

---

### 9. Composite（合成模式）

**一句話定義**：將物件組合成樹狀結構，讓客戶端對單一物件和組合物件採用相同的操作方式。

**使用時機**：
- 需要表示「部分-整體」的層次結構（如檔案系統、組織架構）
- 希望客戶端對葉節點和複合節點一視同仁

**核心角色**：
- `Component`（抽象）：宣告所有節點的共同操作
- `Leaf`：沒有子節點的末端節點
- `Composite`：有子節點的複合節點，實作子節點管理

---

### 10. Decorator（裝飾者模式）

**一句話定義**：動態地為物件添加新職責，比繼承更靈活的功能擴展方式。

**使用時機**：
- 需要在不修改類別的情況下擴展功能
- 需要動態、組合式地添加行為（如串流 I/O 的各種包裝）
- 繼承會導致類別爆炸（子類別太多）

**核心角色**：
- `Component`（介面）：物件的基本介面
- `ConcreteComponent`：被裝飾的原始物件
- `Decorator`（抽象）：持有 `Component` 參考
- `ConcreteDecorator`：添加具體的裝飾行為

---

### 11. Facade（外觀模式）

**一句話定義**：為複雜子系統提供一個簡化的統一介面。

**使用時機**：
- 子系統複雜，客戶端只需要少數核心功能
- 想為分層架構的每層提供入口點
- 想降低客戶端與子系統間的耦合

**核心角色**：
- `Facade`：對外的簡化介面，組合並協調子系統
- `Subsystem Classes`：實際完成工作的子系統類別

---

### 12. Flyweight（享元模式）

**一句話定義**：透過共享大量細粒度物件的公共狀態，有效支援大量物件的建立。

**使用時機**：
- 系統中存在大量相似物件，造成記憶體壓力
- 物件的大部分狀態可以外部化（extrinsic）
- 例如：文字編輯器中每個字元、遊戲中的樹木

**核心角色**：
- `Flyweight`（介面）：宣告接受外部狀態的操作
- `ConcreteFlyweight`：儲存可共享的內部狀態
- `FlyweightFactory`：管理 Flyweight 物件池
- `Client`：維護外部狀態，按需取用 Flyweight

---

### 13. Proxy（代理模式）

**一句話定義**：為另一個物件提供代理或佔位符，以控制對其的訪問。

**使用時機**：
- 遠端代理（Remote Proxy）：代表遠端伺服器上的物件
- 虛擬代理（Virtual Proxy）：延遲建立開銷大的物件
- 保護代理（Protection Proxy）：控制訪問權限
- 快取代理（Caching Proxy）：快取操作結果

**核心角色**：
- `Subject`（介面）：`RealSubject` 和 `Proxy` 的共同介面
- `RealSubject`：實際執行工作的物件
- `Proxy`：持有 `RealSubject` 參考，控制訪問

---

## 三、行為型模式（Behavioral Patterns）

> **共同目的**：定義物件之間的通訊方式和職責分配。

---

### 14. Chain of Responsibility（責任鏈模式）

**一句話定義**：讓請求沿著處理者鏈傳遞，直到某個處理者處理它為止。

**使用時機**：
- 有多個物件可能處理同一個請求，且處理者在執行期才確定
- 想在不指定接收者的情況下向多個物件發送請求
- 例如：審批流程、事件冒泡、日誌等級過濾

**核心角色**：
- `Handler`（抽象）：宣告處理請求的介面，持有下一個 handler 的參考
- `ConcreteHandler`：若能處理則處理，否則轉給下一個
- `Client`：向鏈的起點發送請求

---

### 15. Command（命令模式）

**一句話定義**：將請求封裝成物件，使其可以參數化、排隊、記錄或撤銷。

**使用時機**：
- 需要支援撤銷/重做（Undo/Redo）
- 需要將請求排入隊列或延遲執行
- 需要將操作記錄到日誌，以便崩潰後恢復
- 例如：GUI 按鈕、交易系統、宏錄製

**核心角色**：
- `Command`（介面）：宣告 `execute()`（和可選的 `undo()`）
- `ConcreteCommand`：封裝 Receiver 的操作
- `Invoker`：呼叫 `Command.execute()`
- `Receiver`：知道如何執行操作的物件

---

### 16. Interpreter（解譯器模式）

**一句話定義**：為語言定義文法表示，並提供解譯器來處理該語言的句子。

**使用時機**：
- 需要解析並執行某種簡單語言或表達式（如 SQL、正則表達式、數學表達式）
- 文法規則相對簡單且穩定

**核心角色**：
- `AbstractExpression`：宣告 `interpret()` 方法
- `TerminalExpression`：文法中的終結符（如字面值）
- `NonterminalExpression`：文法中的非終結符（如加法、乘法）
- `Context`：解譯器所需的全域資訊

---

### 17. Iterator（走訪器模式）

**一句話定義**：提供一種方法，按序訪問聚合物件中的元素，而不暴露其底層實作。

**使用時機**：
- 需要遍歷聚合物件，但不想暴露其內部結構
- 需要支援多種遍歷方式（正向、反向、過濾遍歷）
- 想統一不同集合的遍歷介面

**核心角色**：
- `Iterator`（介面）：宣告 `hasNext()`、`next()` 等
- `ConcreteIterator`：實作特定的遍歷邏輯
- `Aggregate`（介面）：宣告 `createIterator()`
- `ConcreteAggregate`：回傳對應的 Iterator

---

### 18. Mediator（中介者模式）

**一句話定義**：定義一個中介物件來封裝一系列物件之間的互動，降低物件間的直接依賴。

**使用時機**：
- 多個物件之間有複雜的雙向依賴（多對多）
- 想集中控制多個物件的互動邏輯
- 例如：聊天室伺服器、飛機交管系統、UI 表單協調

**核心角色**：
- `Mediator`（介面）：宣告與 Colleague 通訊的方法
- `ConcreteMediator`：實作協調邏輯，持有所有 Colleague 參考
- `Colleague`：每個物件只與 Mediator 溝通，不直接互動

---

### 19. Memento（備忘錄模式）

**一句話定義**：在不破壞封裝的前提下，捕獲並外部化物件的內部狀態，使其可以稍後恢復。

**使用時機**：
- 需要儲存物件的快照以便撤銷（Undo）
- 直接獲取內部狀態會破壞封裝

**核心角色**：
- `Originator`：建立 Memento，也從 Memento 恢復狀態
- `Memento`：儲存 Originator 的內部狀態快照
- `Caretaker`：保管 Memento，但不查看或修改其內容

---

### 20. Observer（觀察者模式）

**一句話定義**：定義物件間一對多的依賴關係，當一個物件狀態改變時，所有依賴者都自動收到通知。

**使用時機**：
- 一個物件的狀態改變需要通知多個其他物件
- 不知道有多少物件需要通知（可動態訂閱/取消）
- 例如：事件系統、MVC 中 Model 通知 View、訊息推播

**核心角色**：
- `Subject`（介面）：宣告 `attach()`、`detach()`、`notify()`
- `ConcreteSubject`：維護狀態，狀態改變時呼叫 `notify()`
- `Observer`（介面）：宣告 `update()` 方法
- `ConcreteObserver`：實作 `update()`，根據 Subject 狀態更新自身

---

### 21. State（狀態模式）

**一句話定義**：允許物件在內部狀態改變時改變其行為，讓物件看起來像改變了它的類別。

**使用時機**：
- 物件的行為取決於其狀態，且必須在執行期根據狀態改變行為
- 有大量的條件式（if/else 或 switch）根據狀態執行不同操作
- 例如：自動販賣機、訂單狀態、交通號誌

**核心角色**：
- `Context`：持有當前 State 物件，委派狀態相關行為給它
- `State`（介面）：宣告所有狀態相關操作
- `ConcreteState`：實作特定狀態下的行為

---

### 22. Strategy（策略模式）

**一句話定義**：定義一系列演算法，將每個封裝起來並使其可互換，讓演算法可以獨立於使用它的客戶端。

**使用時機**：
- 需要在執行期切換演算法（如不同的排序、壓縮、驗證策略）
- 想避免多重條件式來選擇演算法
- 演算法的不同變體應該可以獨立測試

**核心角色**：
- `Context`：持有 Strategy 參考，將演算法委派給它
- `Strategy`（介面）：宣告共同的演算法方法
- `ConcreteStrategy`：實作具體的演算法

---

### 23. Template Method（樣版方法模式）

**一句話定義**：在父類別中定義演算法的骨架，將某些步驟的實作延遲到子類別。

**使用時機**：
- 多個類別有相同的演算法結構，但部分步驟不同
- 想讓子類別在不改變整體流程的情況下覆寫特定步驟
- 例如：資料匯出流程（開啟→讀取→轉換→儲存）

**核心角色**：
- `AbstractClass`：定義 `templateMethod()`（final），呼叫各步驟方法
- `primitiveOperation()`：子類別必須實作的抽象步驟
- `ConcreteClass`：實作各個 `primitiveOperation()`

---

### 24. Visitor（拜訪者模式）

**一句話定義**：讓你在不修改物件結構的前提下，為結構中的元素定義新的操作。

**使用時機**：
- 需要對物件結構中的元素執行許多不同且不相關的操作
- 不想讓這些操作「污染」元素類別
- 例如：AST 語法樹的各種分析、報表生成

**核心角色**：
- `Visitor`（介面）：為每種 ConcreteElement 宣告一個 `visit()` 方法
- `ConcreteVisitor`：實作每個 `visit()` 以執行特定操作
- `Element`（介面）：宣告 `accept(visitor)` 方法
- `ConcreteElement`：實作 `accept()`，呼叫 `visitor.visit(this)`

---

*文件生成日期：2026-05-20*
*本文件為「關鍵字爬蟲整合系統」Design Pattern 實作專案的參考資料。*
